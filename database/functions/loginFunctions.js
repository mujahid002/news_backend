const connectMongo = require("../connectMongo");

const storeLoginDetails = async () => {};

const fetchLoginDetails = async () => {};

module.exports = {
  storeLoginDetails,
  fetchLoginDetails,
};
